import { GetjsoncolKeysPipe } from './getjsoncol-keys.pipe';

describe('GetjsoncolKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new GetjsoncolKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
